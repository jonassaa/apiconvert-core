{
  "inputFormat": "json",
  "outputFormat": "json",
  "rules": [
    {
      "kind": "field",
      "outputPaths": [
        "profile.fullName"
      ],
      "source": {
        "type": "merge",
        "paths": [
          "user.firstName",
          "user.lastName"
        ],
        "mergeMode": "concat",
        "separator": " "
      }
    }
  ]
}
