{
  "inputFormat": "json",
  "outputFormat": "json",
  "rules": [
    {
      "kind": "field",
      "outputPaths": [
        "user.name"
      ],
      "source": {
        "type": "path",
        "path": "name"
      }
    }
  ]
}
