{
  "version": 2,
  "inputFormat": "json",
  "outputFormat": "json",
  "fieldMappings": [],
  "arrayMappings": [
    {
      "inputPath": "items",
      "outputPath": "lines",
      "itemMappings": [
        {
          "outputPaths": ["code", "identifiers.sku"],
          "source": { "type": "path", "path": "sku" }
        },
        {
          "outputPath": "quantity",
          "source": { "type": "path", "path": "qty" }
        }
      ]
    }
  ]
}
