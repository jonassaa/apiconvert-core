{
  "name": "apiconvert-core-tests",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "npm install && npm --prefix ../../../src/apiconvert-core install && npm --prefix ../../../src/apiconvert-core run build",
    "test": "npm run build && node --test dist/conversion-cases.test.js"
  },
  "dependencies": {
    "@apiconvert/core": "file:../../../src/apiconvert-core"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5"
  }
}
