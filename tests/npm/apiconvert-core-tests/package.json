{
  "name": "apiconvert-core-tests",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "npm install && npm --prefix ../../../src/apiconvert-core install && npm --prefix ../../../src/apiconvert-core run build",
    "test": "npm run build && node --test dist/*.test.js",
    "coverage": "npm run build && c8 --allowExternal --all --src ../../../src/apiconvert-core/dist --exclude \"**/*.test.js\" --reporter text-summary --reporter html --reports-dir coverage node --test dist/*.test.js",
    "parity:check": "node ../../../tests/parity/parity-gate.mjs --report tests/parity/parity-report.json --summary tests/parity/parity-summary.json --max-mismatches 0"
  },
  "dependencies": {
    "@apiconvert/core": "file:../../../src/apiconvert-core"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "c8": "^10.1.3",
    "typescript": "^5.4.5"
  }
}
