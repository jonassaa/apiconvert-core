# Streaming

Streaming is used for large inputs where whole-document materialization is unnecessary.

<div class="runtime-dotnet">

## .NET

- `StreamJsonArrayConversionAsync`
- `StreamConversionAsync` with `JsonArray`, `Ndjson`, `QueryLines`, `XmlElements`

</div>

<div class="runtime-typescript">

## TypeScript

- `streamJsonArrayConversion`
- `streamConversion` with `jsonArray`, `ndjson`, `queryLines`, `xmlElements`

</div>
