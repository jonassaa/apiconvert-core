# Runtime Selector

The top app selector controls runtime-specific content visibility.

## Behavior

- selection persists in `localStorage`
- non-selected runtime blocks are hidden
- shared content remains visible

## Authoring classes

- `.runtime-dotnet`
- `.runtime-typescript`
